<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/menu_navigate"
        android:title="Navigate">

        <menu>
            <item
                android:title="Create"
                android:id="@+id/menu_create"
                android:icon="@drawable/ic_add_character_icon"/>
            <item
                android:title="View"
                android:id="@+id/menu_view"
                android:icon="@drawable/ic_view_character_icon"/>
            <item
                android:title="Dice"
                android:id="@+id/menu_dice"
                android:icon="@drawable/ic_dice_icon"/>
            <item
                android:title="Spells"
                android:id="@+id/menu_spells"
                android:icon="@drawable/ic_spells_icon"/>
            <item
                android:title="Settings"
                android:id="@+id/menu_settings"
                android:icon="@drawable/ic_settings_icon"/>
        </menu>

    </item>



</menu>